export async function renderPublications() { const container = document.getElementById('publications-container'); if (!container) return; const res = await fetch('data/publications.json'); const publications = await res.json(); container.innerHTML = publications.map(pub => `<div class='list-item'><h3><a href='${pub.pdfLink}' target='_blank'>${pub.title}</a> (${pub.year})</h3><p><strong>Authors:</strong> ${pub.authors.join(', ')}</p><p><em>${pub.venue}</em></p></div>`).join(''); }